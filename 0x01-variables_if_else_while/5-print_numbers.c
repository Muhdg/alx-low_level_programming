#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 * and then in uppercase, followed by a new line.
 * Return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 0; n++)
	{
		printf("%d\n", n);
	}
return (0);
}

